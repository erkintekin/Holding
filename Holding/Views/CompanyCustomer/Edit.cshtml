@model EntityLayer.Concrete.CompanyCustomer

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CompanyCustomer</h4>
<hr />
<form asp-action="Edit">
    <input asp-for="CompanyCustomerID" type="hidden" />
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="CompanyID" class="control-label">Company Name</label>
                <select asp-for="CompanyID" asp-items="ViewBag.CompanySelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="CustomerID" class="control-label">Customer Name</label>
                <select asp-for="CustomerID" asp-items="ViewBag.CustomersSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

