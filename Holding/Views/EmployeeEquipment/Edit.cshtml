@model EntityLayer.Concrete.EmployeeEquipment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EmployeeEquipment</h4>
<hr />
<form asp-action="Edit">
    <input type="hidden" asp-for="EmployeeEquipmentID" />
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="EmployeeID" class="control-label">Company Name</label>
                <select asp-for="EmployeeID" asp-items="ViewBag.EmployeeSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="EquipmentID" class="control-label">Department Name</label>
                <select asp-for="EquipmentID" asp-items="ViewBag.EquipmentSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

