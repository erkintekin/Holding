@model EntityLayer.Concrete.CompanyDepartment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CompanyDepartment</h4>
<hr />
<form asp-action="Edit">
    <input type="hidden" asp-for="CompanyDepartmentID" />
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="CompanyID" class="control-label">Company Name</label>
                <select asp-for="CompanyID" asp-items="ViewBag.CompanySelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="DepartmentID" class="control-label">Department Name</label>
                <select asp-for="DepartmentID" asp-items="ViewBag.DepartmentSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

