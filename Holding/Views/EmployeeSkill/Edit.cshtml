@model EntityLayer.Concrete.EmployeeSkill

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EmployeeSkill</h4>
<hr />
<form asp-action="Edit">
    <input type="hidden" asp-for="EmployeeSkillID" />
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="EmployeeID" class="control-label">Employee Name</label>
                <select asp-for="EmployeeID" asp-items="ViewBag.EmployeeSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                <label asp-for="SkillID" class="control-label">Skill Name</label>
                <select asp-for="SkillID" asp-items="ViewBag.SkillSelect" class="form-select">
                    <option disabled selected value="">Seçiniz</option>
                </select>
            </div>
        </div>
    </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

